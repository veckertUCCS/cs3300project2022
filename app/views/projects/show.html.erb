<!DOCTYPE html>
<html lang="en">
<head>
  <title>Bootstrap 5 Website Example</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link 
6
href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" 
rel="stylesheet">
  <script 
src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.j
s"></script>
</head>
<body>
<p style="color: green"><%= notice %></p>

<div class="px-4 py-5 my-5 text-center border border-primary rounded">
    <div class="col-lg-6 mx-auto">
      <%= render @project %>
      <div class="d-grid gap-2 d-sm-flex justify-content-sm-center pt-5">
        <%= link_to "Edit this project", edit_project_path(@project), :class => 'btn btn-primary btn-md px-4 gap-3' %>
        <%= link_to "Back to projects", projects_path, :class => 'btn btn-primary btn-md px-4 gap-3' %>
      </div>
      <div class="pt-2"
        <%= button_to "Destroy this project", @project, method: :delete, :class => 'btn btn-danger btn-md px-4 gap-3' %>
      </div>
    </div>
  </div>
</body>
